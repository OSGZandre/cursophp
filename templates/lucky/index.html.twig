{% extends 'base.html.twig' %}
{% block title %}Hello LuckyController!{% endblock %}
{% block body %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .salvar 
    { 
        text-color: #0000; 
        background-color : #0b7de0ff;
        font-size: 15px;
        border-radius: 8px;
    }
    .salvar:hover 
    {
        background-color : #045eadff;
        cursor: pointer;  
    }
    .usuarios
    {
        background-color: #e1eaebff;
        border-radius: 10px;
        border 10px solid #000000;
    }
    .usuarios:hover
    {
        background-color: #ccd4d4ff;
        cursor: pointer;  
    }


</style>

<div class="example-wrapper">
        <form action="{{ path('app_lucky_save') }}" method="post">  <!-- Mudei para rota de save, POST -->
            <input type="text" name="name" placeholder="Digite seu nome"/> <br />
            <input type="email" name="email" placeholder="Digite seu Email"/> <br />
            <input type="tel" name="telephoneNumber" placeholder="Digite seu Telefone"/> <br />
            <button class="salvar" type="submit">Salvar</button>
        </form>
        <hr />
        <h1>Usuários Cadastrados</h1>
        <div class="usuarios">
            {% if users|length > 0 %}
                {% for row in users %}  <!-- Corrigi para users -->
                    <h2>Nome: {{ row.name }}</h2>
                    <h2>Email: {{ row.email }}</h2>
                    <h2>Telefone: {{ row.telephoneNumber }}</h2>
                    <hr />
                {% endfor %}
            {% else %}
                <p>Nenhum usuário.</p>
            {% endif %}
        </div>
    </div>
{% endblock %}
